TODO
====

From component: answer.js

map state.question.json_str.questiontype_str

dispatch: none


From component/buttonsanswer.js

map: state.question.inferred.button_values

dispatch: submitButtonsAnswer(item)
    where item := item.name == state.question.inferred.button_values.v.button_response_text


From component/checkboxesanswer.js
    map: state.question.inferred.checkbox_values

    dispatch: submitCheckBoxesAnswer()

    dispatch: changeCheckBoxVlaue(item, checked)
        where item := item.name == state.question.inferred.checkbox_values.v.checkbox_answer_response_text
                      item.result == state.question.inferred.checkbox_values.v.checkbox_answer_checked

                      checked = new_value

From component/slideranswer.js

    map state.question.inferred.slider_values.{slider_value, slider_max, slider_min}

    dispatch: updateSliderValue(value)
        where: value is the value of the slide := state.question.inferred.slider_values.slider_value

    dispatch: submitSliderAnswer()

from component/textanswer.js

    map state.question_inferred.text_values.text_answer_text/text_response_text

    dispatch: changeTextAnswer(text)
        where text == text_response_text

    dispatch: submitTextAnswer()


from views/question.js

    map htmlcontent: state.question.json_str.questionprompt_str

    dispatch: skipQuestion()
    